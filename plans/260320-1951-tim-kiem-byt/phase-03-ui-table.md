# Phase 03: Table & UI Effects
Status: ✅ Complete
Dependencies: Phase 02

## Objective
Dựng khung bảng 3 cột hiển thị dữ liệu danh sách, áp dụng CSS chống vỡ layout và thêm hiệu ứng chữ chạy.

## Tasks
- [x] Thiết kế bảng HTML (Table) với 3 cột: "STT", "Tên quy trình", và "Copy".
- [x] Duyệt mảng dữ liệu (dùng block `{#each}`) để render từng dòng.
- [x] Sử dụng Tailwind để giới hạn độ rộng tối đa của cột 2 ("Tên quy trình") bằng `max-w` và `truncate`.
- [x] Áp dụng thêm CSS để tạo hiệu ứng Marquee tự động cuộn chữ mượt mà cho các tên bị quá dài vượt ngoài viền (bảo vệ bảng 100% không bị ngang phình).
- [x] Nút "Copy" hiển thị ở cột 3 nhưng tạm thời chưa gắn logic.

